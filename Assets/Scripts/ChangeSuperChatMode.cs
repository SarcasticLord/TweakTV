using Unity.VisualScripting;
using UnityEngine;

public class ChangeSuperChatMode : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created'
    public string[] modes = {"GeneralGameIntro", "AsylumIntro", "SubwayIntro", "TweakHQIntro", "Time", "Hints" };
    
}
